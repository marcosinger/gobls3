version: "3"

services:

  app:
    build: .
    volumes:
      - ~/Dropbox/zhikiri.info:/blog
    depends_on:
      - mongo

  mongo:
    image: mongo:3.5
    volumes:
      - ~/docker-shared/gobls3/mongo:/data/db
    ports:
      - "27017:27017"
